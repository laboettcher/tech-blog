{{> post-details post}}

{{#if loggedIn}}
<form class="comment-form">
  <div>
    <textarea name="comment-body"></textarea>
  </div>

  <div>
    <button type="submit">Add Comment</button>    
  </div>
</form>
{{/if}}

{{> comment-details post.comments}}

{{#if loggedIn}}
<script src="/js/comment.js"></script>
{{/if}}